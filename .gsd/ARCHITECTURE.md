# Architecture

> Auto-generated by /map on 2026-03-24

## Overview

This is a modern React Single Page Application (SPA) designed as a static website, likely a dental clinic or similar service business website. It utilizes React Router for navigation without full page reloads, TailwindCSS for styling, and GSAP for advanced animations.

```
┌─────────────────────────────────────────┐
│         [index.html & main.jsx]         │
├─────────────────────────────────────────┤
│    [App.jsx (React Router Setup)]       │
├─────────────────────────────────────────┤
│   [Pages Layer (Home, About, etc.)]     │
├─────────────────────────────────────────┤
│  [Components Layer (Nav, Footer, etc)]  │
└─────────────────────────────────────────┘
```

## Components

### Core Application
- **Purpose:** Sets up global providers, routing, and layout structure.
- **Location:** `src/App.jsx`, `src/main.jsx`
- **Dependencies:** `react-router-dom`, React, `src/index.css`

### Pages
- **Purpose:** Full page views representing distinct sections of the application.
- **Location:** `src/pages/`
  - `Home.jsx`
  - `About.jsx`
  - `Services.jsx`
  - `Reviews.jsx`
  - `Visit.jsx`
  - `Book.jsx`

### Shared Components
- **Purpose:** Reusable UI elements used across different pages.
- **Location:** `src/components/`
  - `Layout.jsx` (Global structural layout wrapper)
  - `Navbar.jsx` (Top navigation)
  - `Footer.jsx` (Bottom footer)
  - `Hero.jsx` (Banner/Hero section)
  - `Chatbot.jsx` (Interactive chatbot widget)

## Data Flow

1. User visits application entry point (`index.html`).
2. React mounts the application via `main.jsx` and initializes `react-router-dom` in `App.jsx`.
3. The selected Route renders its corresponding Page component wrapped within `Layout.jsx` which provides the global `Navbar` and `Footer`.
4. Page components render static content and interactive elements. No external API data fetching was detected; content appears statically coded.

## Integration Points

No external database or backend API integrations were detected in the source code.

## Technical Debt

- [ ] None explicitly marked with TODO/FIXME in the parsed files.

## Conventions

**Naming:** React components use PascalCase (e.g. `Navbar.jsx`, `Home.jsx`).
**Structure:** Traditional React directory structure with `src/components` for shared elements and `src/pages` for route-level components.
**Styling:** Global css provided via `index.css` and utility classes scattered via `tailwindcss`.
